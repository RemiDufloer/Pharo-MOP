SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'MetaObjectProtocol',
      #directory : 'src',
      #load : 'default',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true
    }
  ],
  #testing : { 
  	#packages: [ 'MetaObjectProtocol.*' ]
  }
}